import 'package:flutter/material.dart';

class Serce extends StatelessWidget {
  const Serce({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orangeAccent,
    );
  }
}
